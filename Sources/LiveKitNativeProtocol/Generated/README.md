Generated Swift protobuf sources from the client signaling subset of
`livekit/protocol` live here.

The generated files are intentionally vendored so package users do not need a
local `protoc` installation.

Regenerate with:

```sh
scripts/generate_protocol_sources.sh
```
